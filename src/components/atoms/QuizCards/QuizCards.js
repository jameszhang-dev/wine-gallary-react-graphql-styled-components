import styled from 'styled-components';

import breakpoints from '../../../styles/breakpoints';

const QuizCards = styled.div`
  ${breakpoints.smUp} {
    display: flex;
  }
`;

export default QuizCards;
